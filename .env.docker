# --- Sicherheit ---
SECRET_KEY=change-me
DOWNLOAD_HMAC_SECRET=also-change-me

# --- Admin Login ---
ADMIN_USERNAME=admin
ADMIN_PASSWORD=setze-ein-sehr-langes-passwort

# --- App ---
FLASK_DEBUG=0
PREFERRED_URL_SCHEME=https

# --- Storage & DB (Container-intern, persistent über Volume /data) ---
STORAGE_DIR=/data/storage
DATABASE_URL=sqlite:////data/app.db

# --- CORS ---
# leer = "*" nur für /api/* (wie in get_cors_resources())
CORS_ORIGINS=

